import React from 'react';
import MessengerApp from '../Messenger/src/MessengerApp'; // adjust path if needed

const MessengerWrapper = () => {
  return <MessengerApp />;
};

export default MessengerWrapper;
